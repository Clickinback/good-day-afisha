import { Prisma } from "@prisma/client";
import { prisma } from "@/lib/prisma";
import { calculatePublicationConfidence } from "./confidence";
import { PUBLICATION_POLICY_VERSION } from "./config";

export type PublicationResult={eventId:string;score:number;decision:"AUTO_PUBLISH"|"MODERATION"|"HOLD";status:"PUBLISHED"|"PENDING"|"DRAFT"};
export async function evaluateEventForPublication(eventId:string):Promise<PublicationResult>{const event=await prisma.event.findUniqueOrThrow({where:{id:eventId},include:{sources:{include:{source:true}},organizer:true}});if(event.additionMethod!=="AUTOMATIC")throw new Error("Автопубликация разрешена только для AUTOMATIC событий");if(event.status==="PUBLISHED")return {eventId,score:Number(event.publishConfidence??0),decision:"AUTO_PUBLISH",status:"PUBLISHED"};const sourceScores=[...new Map(event.sources.map(link=>[link.sourceId,Number(link.source.trustScore)])).values()];const breakdown=calculatePublicationConfidence({aiConfidence:Number(event.confidence??0),sourceTrustScores:sourceScores,quality:{hasTitle:Boolean(event.title),hasDescription:Boolean(event.description),hasImage:Boolean(event.imageUrl),hasLocation:Boolean(event.venueId||event.address),hasDateTime:Boolean(event.startsAt),hasPrice:event.isFree||event.priceMin!==null,hasAge:Boolean(event.ageRestriction),hasTicket:Boolean(event.ticketUrl),hasOrganizer:Boolean(event.organizerId),hasEnd:Boolean(event.endsAt)}});const next=breakdown.decision==="AUTO_PUBLISH"?{status:"PUBLISHED" as const,moderationStatus:"APPROVED" as const,publishedAt:event.publishedAt??new Date()}:breakdown.decision==="MODERATION"?{status:"PENDING" as const,moderationStatus:"PENDING" as const,publishedAt:event.publishedAt}:{status:"DRAFT" as const,moderationStatus:"PENDING" as const,publishedAt:event.publishedAt};await prisma.$transaction([prisma.event.update({where:{id:eventId},data:{...next,publishConfidence:breakdown.total,publicationEvaluatedAt:new Date(),publicationPolicyVersion:PUBLICATION_POLICY_VERSION}}),prisma.moderationLog.create({data:{eventId,actorId:"publication-policy",action:breakdown.decision==="AUTO_PUBLISH"?"PUBLISHED":"EDITED",before:{status:event.status,publishConfidence:Number(event.publishConfidence??0)},after:{policyVersion:PUBLICATION_POLICY_VERSION,...breakdown} as Prisma.InputJsonObject}})]);return {eventId,score:breakdown.total,decision:breakdown.decision,status:next.status}}
export async function evaluatePublicationBatch(limit=50){const rows=await prisma.event.findMany({where:{additionMethod:"AUTOMATIC",status:{in:["DRAFT","PENDING"]}},select:{id:true},orderBy:{createdAt:"asc"},take:Math.min(Math.max(limit,1),200)});const results:PublicationResult[]=[];for(const row of rows)results.push(await evaluateEventForPublication(row.id));return results}

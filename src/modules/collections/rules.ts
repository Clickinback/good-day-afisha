import { z } from "zod";
export const collectionRuleSchema=z.object({period:z.enum(["today","tomorrow","weekend","week","upcoming"]).default("upcoming"),category:z.string().min(1).optional(),free:z.boolean().optional(),kids:z.boolean().optional(),evening:z.boolean().optional(),limit:z.number().int().min(1).max(50).default(7)}).strict();
export type CollectionRule=z.infer<typeof collectionRuleSchema>;
export type CollectionCandidate={id:string;startsAt:Date;categorySlug:string;isFree:boolean;ageRestriction:string|null};
export function selectCandidates(rows:CollectionCandidate[],rule:CollectionRule,excluded=new Set<string>()){return rows.filter(row=>!excluded.has(row.id)&&(!rule.category||row.categorySlug===rule.category)&&(!rule.free||row.isFree)&&(!rule.kids||row.categorySlug==="kids"||row.ageRestriction==="0+")&&(!rule.evening||row.startsAt.getHours()>=17)).sort((a,b)=>+a.startsAt-+b.startsAt).slice(0,rule.limit)}
export const defaultCollectionTemplates=[{slug:"today",title:(city:string)=>`Куда сходить сегодня в ${city}`,description:"Актуальные события на сегодня",rule:{period:"today",limit:7}},{slug:"weekend",title:(city:string)=>`7 событий на выходных в ${city}`,description:"Планы на ближайшие выходные",rule:{period:"weekend",limit:7}},{slug:"kids",title:(city:string)=>`Куда сходить с детьми в ${city}`,description:"Семейные события и занятия",rule:{period:"week",kids:true,limit:7}},{slug:"free-week",title:(city:string)=>`Бесплатные мероприятия недели в ${city}`,description:"События со свободным входом",rule:{period:"week",free:true,limit:7}},{slug:"evening",title:(city:string)=>`Что делать вечером в ${city}`,description:"События после 17:00",rule:{period:"today",evening:true,limit:7}}] as const;

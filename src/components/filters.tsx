import Link from "next/link";
import { ArrowRight, X } from "lucide-react";
import { categories } from "@/data/demo";
export function Filters({city, active, q}:{city:string;active?:string;q?:string}) { return <div className="filters"><form className="search-form" action={`/${city}`} role="search"><input aria-label="Поиск мероприятий" name="q" defaultValue={q} placeholder="Концерт, место или событие…"/><button>Найти</button></form>{q?<Link className="clear-search" href={`/${city}`}><X size={14}/>Сбросить поиск</Link>:null}<div className="filter-heading"><b>Категории</b><span>Листайте <ArrowRight size={13}/></span></div><nav className="chips" aria-label="Категории мероприятий"><Link aria-current={!active?"page":undefined} className={!active?'active':''} href={`/${city}`}>Все</Link>{categories.map(c=><Link aria-current={active===c.slug?"page":undefined} className={active===c.slug?'active':''} key={c.slug} href={`/${city}/${c.slug}`}><span>{c.icon}</span>{c.name}</Link>)}</nav></div> }

import { Construction } from "lucide-react";
import { notFound } from "next/navigation";
const sections:Record<string,[string,string]>={sources:["Источники","Управление сайтами, каналами, RSS и API."],cities:["Города","География платформы и часовые пояса."],venues:["Площадки","Адреса и постоянные места проведения."],categories:["Категории","Таксономия событий и порядок отображения."],organizers:["Организаторы","Учреждения, команды и контактные данные."],"collector-runs":["История сборов","Результаты запусков автоматических сборщиков."],errors:["Ошибки системы","Журнал технических ошибок и повторных попыток."]};
export default async function SectionPage({params}:{params:Promise<{section:string}>}){const {section}=await params;const data=sections[section];if(!data)notFound();return <><header className="admin-top"><div><span className="eyebrow coral">Следующий этап</span><h1>{data[0]}</h1><p>{data[1]}</p></div></header><section className="admin-card admin-empty"><Construction/><h2>Раздел подготовлен</h2><p>Модель данных и навигация готовы. Рабочие операции появятся в PHASE 4.</p></section></>}

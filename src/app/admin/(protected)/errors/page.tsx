import { AlertOctagon,Check } from "lucide-react";
import { resolveSystemError } from "@/app/admin/actions";
import { prisma } from "@/lib/prisma";
export default async function ErrorsPage(){let errors:Awaited<ReturnType<typeof prisma.systemError.findMany>>=[];try{errors=await prisma.systemError.findMany({where:{resolvedAt:null},orderBy:{lastSeenAt:"desc"},take:100})}catch{}return <><header className="admin-top"><div><span className="eyebrow coral">Operations</span><h1>Ошибки системы</h1><p>Сгруппированные сбои collectors, parser и фоновых задач.</p></div></header><section className="error-list">{errors.map(error=><article className="admin-card system-error" key={error.id}><AlertOctagon/><div><span>{error.component}{error.operation?` · ${error.operation}`:""}</span><h2>{error.message}</h2><p>Последний раз: {error.lastSeenAt.toLocaleString("ru-BY")} · повторов: {error.occurrenceCount}</p>{error.stack&&<details><summary>Stack trace</summary><pre>{error.stack}</pre></details>}</div><form action={resolveSystemError}><input type="hidden" name="id" value={error.id}/><button><Check size={15}/>Решено</button></form></article>)}{errors.length===0&&<section className="admin-card admin-empty"><span>✓</span><h2>Активных ошибок нет</h2><p>Система работает штатно.</p></section>}</section></>}

import { createVenue,deleteVenue } from "@/app/admin/reference-actions";import { prisma } from "@/lib/prisma";
export default async function VenuesPage(){const [rows,cities]=await Promise.all([prisma.venue.findMany({include:{city:true,_count:{select:{events:true}}},orderBy:{name:"asc"}}),prisma.city.findMany({where:{active:true},orderBy:{name:"asc"}})]);return <><header className="admin-top"><div><span className="eyebrow coral">Инфраструктура</span><h1>Площадки</h1><p>Постоянные места проведения с адресами и городом.</p></div></header><form action={createVenue} className="admin-card reference-form"><label>Название<input name="name" required/></label><label>Slug<input name="slug" required pattern="[a-z0-9-]+"/></label><label>Город<select name="cityId" required>{cities.map(city=><option key={city.id} value={city.id}>{city.name}</option>)}</select></label><label>Адрес<input name="address"/></label><label>Сайт<input name="websiteUrl" type="url"/></label><button>Добавить площадку</button></form><section className="admin-card table-card"><div className="admin-table reference-table"><div className="tr table-head"><span>Площадка</span><span>Город</span><span>Адрес</span><span>События</span><span></span></div>{rows.map(row=><div className="tr" key={row.id}><span><b>{row.name}</b><small>{row.slug}</small></span><span>{row.city.name}</span><span>{row.address??"—"}</span><span>{row._count.events}</span><span>{row._count.events===0&&<form action={deleteVenue}><input type="hidden" name="id" value={row.id}/><button className="icon-action">Удалить</button></form>}</span></div>)}</div></section></>}

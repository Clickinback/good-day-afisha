import type { Prisma } from "@prisma/client";
import { Activity,CheckCircle2,Clock3,XCircle } from "lucide-react";
import { prisma } from "@/lib/prisma";
type Run=Prisma.CollectorRunGetPayload<{include:{source:true}}>;
export default async function CollectorRunsPage(){let runs:Run[]=[];try{runs=await prisma.collectorRun.findMany({include:{source:true},orderBy:{startedAt:"desc"},take:100})}catch{}return <><header className="admin-top"><div><span className="eyebrow coral">Operations</span><h1>История сборов</h1><p>Каждый запуск, его результат и счётчики idempotency.</p></div></header><section className="run-list">{runs.map(run=><article className="admin-card run-card" key={run.id}><i>{run.status==="SUCCEEDED"?<CheckCircle2/>:run.status==="RUNNING"?<Clock3/>:run.status==="FAILED"?<XCircle/>:<Activity/>}</i><div><b>{run.source.name}</b><span>{run.startedAt.toLocaleString("ru-BY")} · {run.finishedAt?`${Math.round((+run.finishedAt-+run.startedAt)/1000)} сек.`:"выполняется"}</span></div><dl><div><dt>Найдено</dt><dd>{run.foundCount}</dd></div><div><dt>Создано</dt><dd>{run.createdCount}</dd></div><div><dt>Пропущено</dt><dd>{run.skippedCount}</dd></div><div><dt>Ошибки</dt><dd>{run.errorCount}</dd></div></dl>{run.errorMessage&&<p>{run.errorMessage}</p>}</article>)}{runs.length===0&&<section className="admin-card admin-empty"><Activity/><h2>Запусков пока нет</h2><p>История появится после подключения первого collector.</p></section>}</section></>}
